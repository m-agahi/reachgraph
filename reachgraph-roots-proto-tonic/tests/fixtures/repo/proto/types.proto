// Messages only, no service. It contributes no root and is still covered:
// coverage answers "what did you look at", not "what produced output".
syntax = "proto3";

package acme.api.v1;

message Widget {
  string id = 1;
}
